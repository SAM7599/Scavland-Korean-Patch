# 보안 정책 (Security Policy)

## 이 프로젝트는 무엇인가요?

Scavland 한글패치는 게임 [Scavland]의 **비공식 팬 제작 한국어 번역 패치**입니다.
게임 개발사·배급사와는 아무 관련이 없으며, 순수하게 커뮤니티 자원봉사로 만들어졌습니다.

패치가 실제로 하는 일은 다음이 전부입니다.

- 게임 폴더 안의 텍스트 데이터(대사, UI, 아이템 설명 등)를 한국어로 교체
- 한글이 깨지지 않도록 폰트 리소스 교체
- 게임 코드(`Assembly-CSharp.dll`) 안에서 캐릭터 이름 등 하드코딩된 문자열을 한국어로 바꾸는 최소한의 수정

**인터넷 통신, 사용자 정보 수집, 계정 정보 접근, 게임 폴더 밖 파일 수정은 전혀 하지 않습니다.**
설치 프로그램(`Install_KR_Patch.bat` → `installer.ps1`)은 지정된 게임 설치 폴더 안의 파일만
복사·교체하며, 실행하면 먼저 이용 안내 및 주의사항을 보여주고 `Y`로 동의해야만 다음 단계로
진행합니다. 설치 전에는 원본 파일을 자동으로 백업해서, "되돌리기" 기능으로 언제든 원상복구할
수 있습니다.

## 다운로드할 때 "위험한 파일"이라는 경고가 떠요

브라우저(크롬 등)나 백신 프로그램이 이 패치를 의심하는 데는 몇 가지 구체적인 이유가 있습니다.

- **게임 코드 파일(dll)을 통째로 교체함** - `Assembly-CSharp.dll`을 새 파일로 바꿔치기하는
  방식 자체가 실제 악성코드의 감염 수법과 비슷해서, 백신이 파일 내용이 아니라 이 "행동
  패턴"만 보고 의심합니다.
- **설치 프로그램이 스크립트(PowerShell)로 되어있음** - 스크립트 파일은 해킹에도 자주
  악용되어 백신이 유독 예민하게 감시합니다.
- **다운로드 수가 적어 "평판"이 낮음** - 대형 게임회사 프로그램과 달리 소수만 받는 개인
  배포 파일이라 평판(reputation) 시스템에서 "출처 불명"으로 분류됩니다.
- **정식 코드 서명(인증서)이 없음** - 유료 코드 서명 인증서가 없어 "제작자 증명이 안 되는
  파일"로 취급됩니다.

이는 실제 악성 행위 여부와 상관없이 뜨는 **자동 오탐(False Positive)**이며, 위에서 설명한
것처럼 이 패치는 네트워크 통신이나 시스템 설정 변경을 하지 않습니다. 더 자세한 설명과
백신 예외 처리 방법은 저장소 [README](../../#백신-프로그램이-위험한-파일로-잡을-때-오탐입니다-안심하세요)를
참고해 주세요.

그래도 안심이 되지 않는다면 다운받은 파일을 [VirusTotal](https://www.virustotal.com)에 올려
여러 백신 엔진으로 한 번에 검사해 보실 수 있습니다.

## 안전하게 받는 방법

- **반드시 이 저장소의 [Releases](../../releases) 페이지에서만** 다운로드하세요.
- 다른 카페·커뮤니티·토렌트 등에 재업로드된 파일은 내용이 바뀌었는지 확인할 방법이 없으므로
  사용을 권장하지 않습니다.
- 배포 zip 파일에는 비밀번호가 걸려 있습니다(**`0000`**, 릴리스 설명에도 공개되어 있습니다).
  이는 파일을 숨기기 위한 것이 아니라, 크롬 등 브라우저가 압축 안의 실행 파일만 보고 자동으로
  검사·삭제하는 것을 막기 위한 조치입니다 - 비밀번호가 걸려 있으면 이런 자동 스캔 자체가
  되지 않습니다.
- 각 릴리스 설명에는 배포 zip 파일의 **SHA-256 체크섬**을 함께 게시합니다. 아래 명령어로
  직접 받은 파일의 체크섬을 확인해 릴리스 페이지의 값과 비교해 보세요.

  ```
  # Windows (PowerShell)
  certutil -hashfile 받은파일.zip SHA256

  # macOS / Linux
  shasum -a 256 받은파일.zip
  ```

  두 값이 다르면 파일이 변조되었거나 잘못 받은 것이니 사용하지 말고 신고해 주세요.

## 지원 버전

패치는 게임 업데이트에 맞춰 계속 새 버전이 나오므로, **가장 최근에 배포된 버전만 지원합니다.**
오래된 버전에서 발생한 문제는 최신 버전으로 업데이트한 뒤 다시 확인해 주세요.

| 버전 | 지원 여부 |
| --- | --- |
| 최신 릴리스 | ✅ 지원 |
| 이전 버전 | ❌ 지원 종료 |
